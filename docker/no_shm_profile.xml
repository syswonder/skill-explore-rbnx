<?xml version="1.0" encoding="UTF-8" ?>
<!--
SPDX-License-Identifier: MulanPSL-2.0

FastRTPS profile that disables the SHM transport.

Why: with --network host + --ipc=host, sim and mapping containers
share /dev/shm. But FastRTPS's SHM transport uses port-hash-named
shm files locked by the writer participant's GUID; cross-container
data transfer through these locks does not work reliably (discovery
goes through fine over UDP, but `topic echo` / `topic hz` time out
even though both endpoints see each other).

Forcing UDPv4-only for both sides removes the SHM path entirely;
FastRTPS still uses UDP unicast for data delivery, which is fast
enough on loopback / host-net and avoids the SHM-namespace mess.

Apply via FASTRTPS_DEFAULT_PROFILES_FILE pointing to this file.
-->
<profiles xmlns="http://www.eprosima.com/XMLSchemas/fastRTPS_Profiles">
  <transport_descriptors>
    <transport_descriptor>
      <transport_id>udp_only</transport_id>
      <type>UDPv4</type>
    </transport_descriptor>
  </transport_descriptors>

  <participant profile_name="participant_no_shm" is_default_profile="true">
    <rtps>
      <userTransports>
        <transport_id>udp_only</transport_id>
      </userTransports>
      <useBuiltinTransports>false</useBuiltinTransports>
    </rtps>
  </participant>
</profiles>
